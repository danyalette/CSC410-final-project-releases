input x : int;
input y : int;
input z : int;
input w : int;
input b : bool;
define hmax : int = x > y ? x : y;
define c : int = hmax; // Var in the rule of G is x or y
assert ((c >= x && c >= y) && (c = x || x = y));
