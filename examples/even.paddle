input x : int;
// Holes
hole h : int [
        G : int -> G + G | G % H | H * G | H | Var;
        H : int -> Integer
];
// Synthesize something even
define a : int = h;
define is_even : bool = h % 2 = 0;
assert (is_even);
